#ansible-dynamic-inventory: fetching 30 servers private ip addresses
#ansible-inventory -i aws_ec2.yml --graph

plugin: amazon.aws.aws_ec2
regions:
- us-east-1 
filters:
  tag: Name:nginx
  instance: state_name:running
hostnames:
- private-ip-address
compose:
  ansible_host: private_ip_address
